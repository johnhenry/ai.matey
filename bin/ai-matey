#!/usr/bin/env node

/**
 * AI.Matey CLI Executable
 *
 * Main entry point for the ai-matey command.
 */

import('../dist/esm/cli/main.js').catch((error) => {
  console.error('Fatal error:', error);
  process.exit(1);
});
