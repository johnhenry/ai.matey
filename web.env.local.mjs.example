  /**
   * The `default` export contains commented-out API keys for various AI services.
   * These keys are placeholders and should be replaced with actual API keys
   * when configuring the application for accessing the corresponding services.
   * The services include OpenAI, Gemini, Anthropic, Mistral, Groq, Nvidia, and Ollama.
   */
   * This is a default configuration for the demo, it's not supposed to be used in production.
   * You should replace the values with your own API keys.
   *
   */
export default {
  api_keys: {
    openai:"",
    lmstudio: "",
    gemini: "",
    anthropic:"",
    mistral: "",
    groq: "",
    nvidia:"",
    ollama: "",
  }
}